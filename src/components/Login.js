import React from "react";
import { Box } from "@mui/system";

const Login = () => {
  return (
    <div style={{ width: "100%" }}>
      <Box
        sx={{
          display: "flex",
          justifyContent: "center",
          alignItems: "center",
        }}
      >
        Login
      </Box>
    </div>
  );
};

export default Login;
