import React from "react";
import { Box } from "@mui/system";

const Chat = () => {
  return (
    <Box
      sx={{
        display: "flex",
        justifyContent: "center",
      }}
    >
      Chat
    </Box>
  );
};

export default Chat;
